# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Rust build output
src-tauri/target/
src-tauri/bin/screenshot/target/

# Large binaries
src-tauri/resources/ffmpeg/
src-tauri/resources/ocr/

# Excalidraw 的手写字体(14MB)。它是 node_modules 的副本,由
# scripts/gen-hand-font-css.mjs 在 dev/build 前自动生成,不进仓库。
public/excalidraw/
public/fonts/handwriting.css

# Build output
release/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 参考素材(第三方作品,不入库)
_reference/wisp-particle-orb/

# AI 助手的本地配置与第三方 skill —— 留在本机用，不进仓库。
# 2026-08-28 从历史里移除：.claude/settings.local.json 带着本机用户名，
# 而 .claude/skills 和 .agents/skills 是同样 304 个第三方 skill 存了两份。
.claude/
.agents/

# Rust 编译产物（另一处 target，之前漏挡进了历史）
native/target/
