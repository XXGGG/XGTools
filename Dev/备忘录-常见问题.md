## 常见问题记录 (Troubleshooting)

### 🔴 端口被占用或权限错误 (EACCES / Port already in use)
**现象**:
启动时报错 `Error: listen EACCES: permission denied` 或 `Error: Port 14xx is already in use`。

**解决方案**:
修改默认端口 (例如改为 **3333**)。

1.  修改 `vite.config.ts`:
    ```typescript
    server: {
      port: 3333,
      strictPort: true,
      host: host || false,
      hmr: host ? {
        protocol: "ws",
        host,
        port: 3334,
      } : undefined,
    }
    ```

2.  修改 `src-tauri/tauri.conf.json`:
    ```json
    "build": {
      "beforeDevCommand": "pnpm dev",
      "devUrl": "http://localhost:3333",
      "beforeBuildCommand": "pnpm build",
      "frontendDist": "../dist"
    }
    ```

### 🟡 VS Code 警告：Unknown at rule @apply
**现象**:
在 `src/style.css` 等文件中，使用 `@apply` 语法时，VS Code 显示黄色警告。

**解决方案**:
在项目根目录下创建 `.vscode/settings.json` 文件：
```json
{
  "css.lint.unknownAtRules": "ignore",
  "scss.lint.unknownAtRules": "ignore"
}
```

### 🟡 红色波浪线 (Rust Analyzer)
- **现象**: 代码能运行，但 VS Code 里全是红线。
- **解决**:
  1. 运行 `cargo check` 确保真的没问题。
  2. 重启 VS Code 或 `Ctrl+Shift+P` -> `Rust Analyzer: Restart server`。