# Changelog

本文档记录项目的所有重要变更。

格式参考 [Keep a Changelog](https://keepachangelog.com/)，版本号遵循 [语义化版本](https://semver.org/)。

## [Unreleased]

## [v0.8.2] - 2026-09-04

### Changed
- 显示方式改名：「阅读模式」→「精简模式」，「写作模式」→「Obsidian 模式」；
  新装默认是精简模式（已经存过设置的照旧）
- 文档顶栏里的按钮缩到 30px，上下留出和目录栏一样的空隙

### Fixed
- 刮选一大段时，正文左右两侧的留白也被一起涂成选区。现在选区只盖文字那一列
- 没标语言的代码块整块字是紫的（主题把它当链接色了）。带语言的块和行内代码本来就没事
- 截图里画框画到一半按 Esc 没反应；现在会丢掉这一笔


## [v0.8.1] - 2026-09-04

### Added
- **宽表格有横向滚动条了**，而且是悬浮的：表格整个在屏幕里就贴在表格底下，
  下半截还在屏幕外就贴在正文可视区底边（磨砂半透明），你在看第 3 行也能拖。
  滚到快到底时两条边重合，滚动条跟着表格一起离开，不会出现两根叠着
- 标签栏右侧多一颗「关闭全部」（超过两个标签才出现，关之前先确认）
- 双击笔记标题可以直接改文件名

### Changed
- 井号照 Obsidian 的规矩：只有「# 」加正文才是标题，光一个 `#`、或者 `#abc`
  都按原样显示。原来 CommonMark 允许「空标题」，一个 `#` 也会被吞成 H1
- 行内 HTML 标记全认：`<b>` `<strong>` `<i>` `<em>` `<s>` `<del>` `<u>` `<ins>`
  `<small>` `<big>` `<sub>` `<sup>` `<mark>` `<kbd>` `<font color>`，样式照浏览器默认
- 笔记目录栏顶部的按钮放大到 44px，和智能体那边一致
- 正文右缘那一列大纲短线平时是灰的，只有正在看的那一节亮主题色 ——
  原来整列都是紫的，分不出哪条是现在。大纲面板本身不变
- 表格滚动条加粗一倍（10 → 20px），原来太细不好抓


## [v0.8.0] - 2026-09-03

### Added
- **录制时长上限**，默认 30 分钟（可选 10 / 30 / 60 / 不限制）。
  到点是**自动停下来并保存**，不是作废 —— 最坏也就是一段长录像被切成两截，
  一点东西都不丢。真正防的是「点了开始就忘了」：那种情况下硬盘会被悄悄写满，
  而且全程没有任何提示。自动停下来时控制条上会换成一个闹钟图标说明原因

### Changed
- **截图工具条改版。** 十四个标注工具原来平铺成一排，加上右边七八个操作按钮，
  一条工具条二十来颗三十见方的小图标 —— 挤、难点，一眼扫不出哪个是哪个。
  现在按「要干的事」收成五组：画形状、画线、写字、打码、擦掉。
  每组只占一颗按钮，显示的是这一组**上次用过的那个**（不是抽象的组图标），
  所以常用路径还是「点一下就用」，没有多一层；右下角一个小三角提示底下还有别的。
  展开的规矩是「点一下用，已经选中了再点一下才展开」（右键也行）——
  悬停自动展开试过，鼠标横穿工具条时一路弹菜单，太烦
- 工具条整体放大：按钮 30 → 36，图标 16 → 18，条高 38 → 48，圆角和阴影跟着调
- 标注工具的名字接进了多语言（原来是写死的中文）


## [v0.7.0] - 2026-09-03

### Added
- **录屏可以带上电脑里的声音了**（不是麦克风，是音箱/耳机正在放的那一路）。
  在「截图 → 录屏」里开关，默认开着，录的时候控制条上会亮一个小喇叭。
  ffmpeg 在 Windows 上做不到这件事（它只有 dshow，而 dshow 只认录音设备），
  所以走的是 Windows 自带的 WASAPI 回环采集，抓到的声音经一条命名管道喂给 ffmpeg。
  **插什么声卡都行，也不用装虚拟声卡驱动**
- 截图工具条里也加了一颗「录制」：框都框好了临时改主意想录一段，
  不用退出去重按一次快捷键

### Fixed
- **双击顶栏最大化没反应。** 代码一直都在，但永远收不到 dblclick ——
  第一下按下就调了「开始拖窗口」，窗口拖拽随即交给系统的模态消息循环，
  后半个双击被系统吞掉了。改成在开拖之前先看这是连击的第几下
- 录完那条结果条不会自己关（要留着给「打开文件夹 / 转 GIF」），
  于是紧接着再录一段时新的控制条**建不出来而且不报错**：屏幕上还是上一段的结果条，
  新这段既没有计时也没有停止按钮
- 控制条要点两下才停得下来：它不抢焦点，于是第一下点击只是用来激活窗口。
  改成 Windows 上悬浮工具条的标准做法（永不抢激活），一下就是一下


## [v0.6.0] - 2026-09-03

### Added
- **框选区域录屏**：`Ctrl + \` 之外新增 `Ctrl + Alt + R`，框一块区域直接录成 MP4。
  框选那一步和截图完全一样 —— 同一套遮罩、同一个选区、同一套窗口吸附和像素吸附，
  只是选完之后出的不是标注工具条，而是一颗「开始录制」
- 录制时屏幕上留一圈琥珀色的框（鼠标穿透，不挡下面的软件）和一条控制条；
  停止后控制条原地变成结果条：打开所在文件夹 / 一键转 GIF
- 录屏设置在「截图」页新开的「录屏」页签里：开关、快捷键、保存位置、帧率。
  默认存到「下载 \ Recordings」
- 托盘菜单和 F1 快捷键一览里都加上了录屏

### Fixed
- 截图页原来自己攒了一份「读三个键 → 更新全部快捷键」，少传的那几个键在后端会被
  当成 null 全部注销 —— 在截图页动一下开关，命令面板的快捷键就没了，而且没有任何报错


## [v0.5.1] - 2026-09-03

### Added
- 「清除格式」有快捷键了：`Ctrl + \`（Google 文档那个）。刮选一段按一下，
  加粗 / 斜体 / 删除线 / 高亮 / 颜色 / 代码 / 注释 全去掉，只留字。
  F1 的快捷键一览里也排上了

### Fixed
- **「清除格式」会把行写坏，而且写坏之后再清也救不回来。**
  旧实现是「见到星号、波浪号、等号就删」，于是把
  `<span style="color:green">` 里那个**等号**也删了 —— 标签当场不成立，
  整行烂在那儿，而且新的一对尖括号它压根不认识，再点也清不掉。
  现在剥的是**记号**不是字符：走解析器认出哪几段是格式，整段拿掉，
  正文里本来就有的 `2 * 3` `a=b` 一个字都不会被误伤（写坏的标签也能收拾了）
- 只刮中字、没刮到记号时清格式只清一半：`**CCC**` 双击选中 CCC 再清，
  出来是 `**CCC`。记号现在是藏着的，本来就刮不到 —— 现在会自己往外扩到
  完整的一段再剥，旁边没刮到的格式不动
- 代码块里的 `**` `<span>` 不再被清格式误伤 —— 那里面本来就是要原样展示的


## [v0.5.0] - 2026-09-03

### Changed
- 行首的 `- ` `1. ` `- [ ] ` `> ` `## ` 现在**刮选不到了**（和 Notion 一样）。
  以前能刮到，于是每一个格式命令都得自己记得躲开它 —— 上色躲一次、加粗躲一次、
  删除线再躲一次，漏一个就把整条列表写坏。与其在每个命令里打补丁，不如让它压根选不进来
- 右键菜单的加粗 / 斜体 / 删除线改成和快捷键走同一份实现。原来各写一份，
  同一类 bug 得修两遍

### Fixed
- 刮选多行再加粗 / 删除线，会写出 `~~第一行\n第二行~~` 这种跨行的记号 ——
  行内记号碰到换行就断了，解析器不认，两行都原样带着波浪线。
  现在跨行时每一行各包一对，整段跟着第一行走（有一行没包就全包上，全包了才全脱掉）


## [v0.4.2] - 2026-09-03

### Fixed
- 刮选带上了行首的 `- ` 再上色，整条列表会塌成一根横杠。记号和它后面那个空格
  必须连着，只挡住 `-` 而让 span 从空格开始，写出来是 `-<span …> 123456</span>`，
  短横线后面紧跟着一个 `<`，markdown 当场就不认它是列表了。
  现在上色一律从这一行的正文列开始，`- ` `1. ` `- [ ] ` `> ` `## ` 都不会被裹进去


## [v0.4.1] - 2026-09-03

### Fixed
- 上色不再切坏别的记号。以前是拿选区直接裹一对 `<span>`，完全不管这段范围横跨了
  什么，于是刮到一半的记号被劈开、两边都不成立：`1**3112312**` 上色变成
  `1**<span …>3112312**</span>`（加粗没了）、`12\`31231\`` 变成两截断掉的代码。
  现在记号本身和「原样展示的整段」（行内代码、代码块、公式）都会被绕开，
  得到的是 `**<span …>加粗</span>**`
- 在 `==高亮==` / `%%注释%%` 看得见的最后一个字后面按回车，会把一对记号劈成两行、
  记号当场原样冒出来。记号是藏起来的，那个位置在文档里其实是闭合记号的**前面** ——
  看不见的东西咬了人一口。现在收起来的记号是一个整体，光标会整个跨过去


## [v0.4.0] - 2026-09-03

这一版的重心在智能体页和笔记编辑：智能体有了「项目」这一层，笔记页补上了格式快捷键、
划词菜单和三档显示方式。命令面板也从启动台里搬出来单独成页。

### Added
- 智能体：项目改成两层（项目类 → 项目），一个项目 = 一个文件夹 + 一次对话
- 智能体：项目绑文件夹分「从笔记库里挑」和「从磁盘上挑」；换文件夹会用新目录重开一轮对话
- 智能体：文件树单击就打开；正文里刮选 + `Alt+K` 把 `@路径#行号` 送进输入框
- 智能体：Skills 库改成「指向目录」，不复制也不做软链；一颗「告诉 AI」把目录写进全局规矩
- 智能体：接过 Claude Code / Codex 的项目（指向同一个文件夹，不复制）
- 智能体：大段文本粘进输入框收成附件条；聊天区消息铺满、输入框浮起
- 命令面板：独立成一页（侧栏下卡片），快捷键和设置页走同一份数据
- 命令面板：输入框最多长到五行；回车之后底下的结果列表收起来；「回车默认翻译」可关
- 笔记：28 条格式快捷键（`Ctrl+B/I/U/E/K`、`Ctrl+1~6`、列表引用分隔线…），按 F1 看全表
- 笔记：右键菜单重排 —— 颜色和文本格式各一格 4×2 图标，段落 / 插入 / 更多收进二级菜单
- 笔记：文本颜色（红橙黄绿蓝靛紫），写的是颜色名，亮暗模式各一套
- 笔记：三档显示方式 —— 写作模式 / 阅读模式 / 源码模式
- 笔记：`%%注释%%` 认得了，压暗 + 收起标记
- 翻译：兜底（和引擎链同时跑，谁快用谁），只收跑在本机的东西
- 截图：设置页加「重启截图」
- 可选插件能卸载了，装卸都带进度条

### Changed
- 翻译设置拆成「引擎顺序」和「兜底」两个页签，说明各收成一句话
- 顶栏让出一整行，引擎状态挪到右上角
- 「自定义接口」改名「AI 自定义接口」

### Fixed
- 笔记：加粗按了没用 —— 选区尾随空格会让 markdown 的强调标记不成立
- 笔记：列表项里的 `<span style="color:red">` 不显示颜色 —— 语法高亮在颜色 span 里面
  又套了一层带 color 的元素，子元素赢过祖先
- 笔记：阅读模式下加粗 / 高亮 / `#` 仍然会露出记号
- 翻译：换颜色会嵌套而不是替换；跨行选区会写出一对跨行的 span
- 翻译：兜底改完不保存
- 托盘菜单偶尔跑到屏幕上一个奇怪的位置，而且怎么点都不消失
- 右键的二级菜单弹不出来、样式改不动
- 翻译设置弹窗里点第二个页签会把弹窗关掉


## [v0.3.0] - 2026-09-01

这一版把 XGTools 从「一堆小工具」推成了日常干活的地方：笔记页、智能体页、命令面板三块大件都落地了。

### Added
- 笔记页：直接管本地 Markdown 库，边打边渲染；文件树拖拽移动、原地改名，正文有右键菜单
- 笔记页：标题折叠（H1–H6），收起状态切标签、切页面回来都还在；右上角一键全展开
- 笔记页：公式、流程图、可视化表格、查找面板；代码块右上角【复制】按钮，写了语言就显示语言名
- 笔记页：支持行内 `<span style="color:blue">` 写法，颜色名跟着亮暗模式走
- 笔记页：字体 / 字号 / 宽度 / 主题色都能调，随包附带三款开源中文手写体
- 笔记页：文件恢复（每两分钟自动存一份），整份复制、刮选部分复制都行
- 大纲面板按标题层级配色；右侧大纲条限定范围，不影响顶部和底部
- 无限画布，和 Obsidian 的 Excalidraw 双向互通
- 智能体页：内置 DeepSeek Harness，模型、权限、归档、markdown 渲染齐了
- 命令面板：一个快捷键找应用、笔记、会话和功能页；兼做一句话翻译
- 文件搜索走系统自带的索引，用户不用装任何东西
- 翻译：本机离线翻译（Bergamot 引擎 + Mozilla 中英模型），可接本机模型
- 翻译：引擎改成可拖排序的优先级链，一个不行自动往下兜底
- 截图：像素级抓框，能框住任务栏一行、输入框、VS Code 的各个栏
- 设置：快捷键总览页，能改键、看占用、一键重注册和恢复默认
- 设置：笔记设置分大段（正文外观 / 图片存放 / 删除笔记时），单页宽度出列表可逐条清
- 打包：`pnpm dist` 把安装包收到根目录 `installer/`

### Changed
- 启动顺序：先按上次的主题铺不透明底和布，材质贴好再换底，不再闪白
- 命令面板整块居中，长高时缓缓上移；回车默认翻译，按 ↓ 选中结果才是打开
- 快捷键：翻译面板和别的键一样有自己的开关，默认 Ctrl+Alt+E；截图 Ctrl+Alt+A、截图翻译 Ctrl+Alt+D
- 笔记设置里「正文铺满整栏」改名「全局全宽度」
- Ctrl+F 的关闭按钮改成右侧上下居中的圆角方块，红底
- 笔记在外部被改动时：本地有没保存的改动才问「覆盖 / 不覆盖」，没改动的直接同步
- 截图翻译整套淡红，不再显示像素尺寸标签

### Fixed
- 笔记：删掉空列表项不再把整片列表格式打乱（制表符缩进的也一样）
- 笔记：撤销撤到头不会把笔记换成上一篇
- 笔记：中文输入法打全角标点（【（，《），光标不再跑到字前面
- 笔记：同一个文件不会开出好几个标签
- 笔记：历史版本的正文能刮选了（`select-text` 被全局的不可选规则压住）
- 截图：出图不再带上选中框那圈虚线和八个把手；回车先把文字落下去；滚轮方向；背景开关不记事
- 云母材质偶尔不画、底面发白；深色主题下启动底面是白的
- 附属窗口会把主界面刚改的设置整份盖回去
- 托盘菜单被任务栏挡住，换成自绘的菜单窗口
- 热键注册失败后会在后台重试
- 补齐编辑器缺的可选语言包，代码块高亮不再刷报错


## [v0.2.0] - 2026-08-25

界面整体重做：窗口 chrome 改为浮空卡片，内容相对整窗居中。

### Added
- 设置页：全新的独立页面，Tabs 分「常规」与「侧边栏」，入口在侧栏底部常驻
- 窗口背景：支持云母 / 亚克力材质，可调底面不透明度
- 侧边栏自定义：拖拽排序、逐项开关、跨组拖动改变归属（工具 / 功能配置两张卡片）
- 首页 Logo 与字标改为粒子渲染，鼠标靠近推散、移开归位
- 键盘桌宠：自动清除开启后可调延时（2–20 秒）

### Changed
- 侧边栏与顶栏改为浮空卡片，覆盖在内容之上，内容相对整个窗口居中
- 顶栏左上角改为 Logo 入口；页面的 Tabs 统一 Teleport 到顶栏
- 窗口控件改为三个圆点（绿 = 最小化 / 黄 = 最大化 / 红 = 关闭）
- 默认窗口 1440×900（16:10），最小 960×600
- 「习惯打卡」从界面隐藏（代码保留）
- 页面切换动画改为缩放淡入淡出

### Removed
- 翻译：移除 Yandex（跳 captcha 墙）与腾讯交互翻译（需 token）两个引擎
- 粒子球，以及仅为它服务的动态壁纸、定时屏保、独立渲染进程
- 早年截图 sidecar 的遗留代码（截图早已改为进程内实现）

### Fixed
- 翻译：Bing 引擎全面失效 —— `edge.microsoft.com/translate/auth` 已被微软下架，
  改为从 `bing.com/translator` 抓 IG 与 AbusePreventionHelper 凭据，走 `ttranslatev3`
- 翻译：免费引擎失败时自动降级到下一个（google → bing → deepl → mymemory），
  不再把单个引擎的失效直接摔给用户；AI 引擎用自己的 key，失败不降级
- 窗口特效完全不生效：`body` 的不透明底把系统材质整层盖住
- 高斯模糊渲染成一片死黑：`ACCENT_ENABLE_BLURBEHIND` 在 Win11 已废弃，改为不提供
- 深色模式下亚克力仍按浅色渲染：`apply_acrylic` 不设置 `DWMWA_USE_IMMERSIVE_DARK_MODE`
- 切回「关闭」时窗口闪一下全透明：清材质与恢复底色的顺序反了
- 页面切换时内容跳动：过渡包裹层在「是/不是包含块」之间反复切换
- 提示条改为右下角浮空 toast，不再插进布局把内容整体下压


## [v0.1.2] - 2026-03-24

### Added
- 翻译：结果区新增朗读按钮，点击朗读译文，再次点击停止
- 翻译：输入区新增朗读按钮，支持朗读原文（自动识别中英文语音）
- 格式转换：视频文件支持提取音频，可直接转换为 mp3 / wav / flac / aac / ogg / m4a

### Changed
- 翻译：朗读按钮改为常驻显示，不再需要悬浮才出现
- 侧栏：隐藏待办清单，仅保留习惯打卡；默认页改为习惯打卡

## [v0.1.1] - 2026-03-xx

### Fixed
- 截图翻译后，选区外鼠标指针从 `text` 改为 `default`（默认箭头）
- 截图进行中重复按快捷键导致无限套娃，现改为取消当前截图
- 截图窗口吸附无法识别自身应用窗口（xcap 库过滤同进程窗口），改用 Win32 API 补充枚举
- 窗口吸附优先级改为按面积排序，小窗口优先于全屏窗口
- 截图背景投影：去除最外层多余透明边距，阴影改为投射在背景面板上（内容浮空感）
- 截图背景投影：内容圆角匹配窗口吸附圆角，修复尖角和边框残留
- 截图背景投影：修复 `destination-out` 亚像素残留黑线
- 窗口吸附截图输出时内缩 1px，去除 DWM 边框残留

### Added
- 截图工具栏新增翻译按钮，无需开启截图翻译快捷键也能直接翻译
- 截图翻译模式下，右键可关闭翻译覆盖层
- 截图标注画完后自动选中，可直接通过样式面板修改颜色、线宽等属性
- 点击已有标注可重新选中并修改样式
- 选中标注后可拖拽移动、拖拽控制点缩放大小
- 绘制/缩放时按住 Shift 可约束为正方形、正圆、45° 线条

### Changed
- 启动台改为无边框全屏（覆盖任务栏），替代系统最大化

## [v0.1.0] - 2026-02-23

### Added
- 暗黑模式 & 暗黑模式持久化
- 开机自启
- 按键显示模块（智能布局、躲避鼠标、自动清除、定位持久化）
- Windows 启动台
- 截图功能（选区截图、标注、投影背景、窗口圆角）
- 截图翻译功能
- 格式转换（图片/音视频）
- Todo 与计时器
- FFmpeg 缺失自动检测与一键下载
